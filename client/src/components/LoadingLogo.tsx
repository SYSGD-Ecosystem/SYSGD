import type React from "react";

const LoadingLogo: React.FC = () => {
	return (
		<div className="inline-block relative">
			<span className="loading-logo-text">SYSGD</span>
		</div>
	);
};

export default LoadingLogo;
